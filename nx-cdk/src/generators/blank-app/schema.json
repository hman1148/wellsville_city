{
  "$schema": "https://json-schema.org/schema",
  "$id": "BlankApp",
  "title": "",
  "type": "object",
  "properties": {
    "appSubDirectory": {
      "type": "string",
      "description": "The subdirectory path for the application",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "Specify the subdirectory for your application (will be placed in the applications directory)"
    },
    "implicitDependencies": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Projects this stack depends on (optional). If specified, the generated stack will include commented examples for cross-stack references.",
      "x-dropdown": "projects"
    }
  },
  "required": ["appSubDirectory"],
  "additionalProperties": false
}
